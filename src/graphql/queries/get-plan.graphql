query GetPlan($id: ID!) {
  plan(where: { id: $id }) {
    expenses
    days
    transportation
    location
    likes
    likesCount
    createdAt
    images
    member {
      name
      image
    }
  }
}
